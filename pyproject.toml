[project]
name = "causal-agents-workshop"
version = "0.1.0"
description = "Build an intelligence agent with causal reasoning over GitHub project health"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "groq>=1.6.0",
    "pygithub>=2.9.1",
    "python-dotenv>=1.2.2",
]
